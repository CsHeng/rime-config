# Hamster3 target sync filter
# Input: build/stage/hamster3/ (merged upstream + overlays)

# Never touch user state
- user.yaml
- installation.yaml
- **.userdb/
- build/
- build/**

# Upstream docs/examples (not runtime)
- custom/
- custom/**

# iOS: do not push UI yaml (let app/device manage it)
- default.custom.yaml
- squirrel.yaml
- weasel.yaml
- hamster.yaml
- hamster.custom.yaml
- "简纯+.trime.yaml"

+ *
