# Weasel (Windows) target sync filter
# Input: build/stage/weasel/ (merged upstream + overlays)

# Never touch user state
- user.yaml
- installation.yaml
- **.userdb/
- build/
- build/**
- lua/tips/
- lua/tips/**

# Upstream docs/examples (not runtime)
- custom/
- custom/**

# Keep target clean: exclude other platform UI yaml
- squirrel.yaml
- hamster.yaml
- "简纯+.trime.yaml"

+ *
