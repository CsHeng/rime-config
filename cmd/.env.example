# Copy to cmd/.env (gitignored) and edit as needed.

# Optional: GitHub API token (avoids rate limit)
# GITHUB_TOKEN=ghp_xxx

# Optional: default platform for update/init (auto|squirrel|weasel|hamster|hamster3|none)
# RIME_PLATFORM=auto

# Optional: override inferred target dir (useful on Windows or for testing)
# RIME_TARGET_DIR=~/Library/Rime

# Optional: where to read repo-local layer from (defaults to repo root)
# RIME_LOCAL_DIR=.

# Optional: run init before update by default (0/1)
# RIME_INIT=0

# Optional: disable rsync --delete (0/1)
# RIME_RSYNC_DELETE=1

# Optional: iCloud app container ids (rarely need to change)
# RIME_HAMSTER_ICLOUD_BASE=iCloud~dev~fuxiao~app~hamsterapp
# RIME_HAMSTER3_ICLOUD_BASE=iCloud~com~ihsiao~apps~Hamster3
